<h1>Specsheets</h1>

<div class="panel panel-default">
  <div class="panel-heading">All Specsheets</div>
    <table class="table">

<!-- TEMPORARY TABLES -->
        <tr>
          <td>Laptops</td>
          <td>101 items</td>
          <td>42 reviews</td>
        </tr>
        <tr>
          <td>Cameras</td>
          <td>75 items</td>
          <td>60 reviews</td>
        </tr>
        <tr>
          <td>Bacon</td>
          <td>21 items</td>
          <td>375 reviews</td>
        </tr>

<!-- WHAT I WACTUALLY WANT TO USE -->
      <% if false %>

      <% @sheets.each do |sheet| %>
        <tr>
          <td><%= link_to @sheet.name, sheet %></td>
          <td><%= @sheet.items.count %> items</td>
          <td><%= @sheet.reviews.count %></td>
        </tr>
      <% end %>
      
      <% end %>

    </table>
</div>